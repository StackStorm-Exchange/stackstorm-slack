description: "This action is auto-generated. See https://api.slack.com/methods/channels.join"
enabled: true
entry_point: run.py
name: channels.join
parameters:
  end_point:
    default: channels.join
    immutable: true
    type: string
  http_method:
    default: POST
    required: true
    type: string
  token:
    required: false
    description: Authentication token bearing required scopes.
    type: string
  name:
    required: true
    description: Name of channel to join
    type: string
  validate:
    required: false
    description: Whether to return errors on invalid channel name instead of modifying it to meet the specified criteria.
    type: string
runner_type: python-script
