description: "This action is auto-generated. See https://api.slack.com/methods/conversations.members"
enabled: true
entry_point: run.py
name: conversations.members
parameters:
  end_point:
    default: conversations.members
    immutable: true
    type: string
  http_method:
    default: GET
    required: true
    type: string
  token:
    required: false
    description: Authentication token bearing required scopes.
    type: string
  channel:
    required: true
    description: ID of the conversation to retrieve members for
    type: string
  cursor:
    required: false
    description: Paginate through collections of data by setting the cursor parameter to a next_cursor attribute returned by a previous request's response_metadata. Default value fetches the first "page" of the collection. See pagination for more detail.
    type: string
  limit:
    required: false
    default: "100"
    description: The maximum number of items to return. Fewer than the requested number of items may be returned, even if the end of the users list hasn't been reached.
    type: string
runner_type: python-script
